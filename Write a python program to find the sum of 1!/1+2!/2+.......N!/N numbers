def factorial(n):
    return 1 if n == 0 or n == 1 else n * factorial(n - 1)

def sum_of_factorials(N):
    return sum(factorial(i) / i for i in range(1, N + 1))
N = 5
print(f"The sum of the series for N={N} is: {sum_of_factorials(N)}")
