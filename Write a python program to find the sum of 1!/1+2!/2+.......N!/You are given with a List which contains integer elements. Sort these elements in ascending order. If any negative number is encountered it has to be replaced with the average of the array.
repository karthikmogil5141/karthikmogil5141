
numbers = [3, -1, 4, 1, -5, 9, 2, -6]
average = sum(num for num in numbers if num >= 0) / len([num for num in numbers if num >= 0])
numbers = [num if num >= 0 else average for num in numbers]

numbers.sort()

print(numbers)
